# Dark Background Audit Report

Generated on: 2025-08-28T02:04:08.773Z

Total matches found: 294

## src/features/analytics/Reports.tsx

**Line 114** (bg-zinc-900):
```
? "bg-zinc-900 text-white"
```

**Line 145** (bg-zinc-900):
```
className="odd:bg-zinc-50/40 dark:odd:bg-zinc-900/40"
```

## src/features/audit/AuditLog.tsx

**Line 383** (bg-zinc-900):
```
<div className="border rounded p-2 bg-white dark:bg-zinc-900">
```

**Line 383** (form-control-dark):
```
<div className="border rounded p-2 bg-white dark:bg-zinc-900">
```

**Line 389** (bg-zinc-900):
```
<div className="border rounded p-2 bg-white dark:bg-zinc-900">
```

**Line 389** (form-control-dark):
```
<div className="border rounded p-2 bg-white dark:bg-zinc-900">
```

## src/features/auth/Login.tsx

**Line 82** (bg-zinc-900):
```
<div className="w-full max-w-md rounded-lg border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-900 p-6 shadow-sm">
```

**Line 82** (form-control-dark):
```
<div className="w-full max-w-md rounded-lg border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-900 p-6 shadow-sm">
```

**Line 101** (bg-zinc-900):
```
className="w-full rounded-md border border-gray-300 dark:border-zinc-700 bg-white dark:bg-zinc-900 px-3 py-2 outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 dark:focus:ring-blue-400 dark:focus:border-blue-400"
```

**Line 101** (form-control-dark):
```
className="w-full rounded-md border border-gray-300 dark:border-zinc-700 bg-white dark:bg-zinc-900 px-3 py-2 outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 dark:focus:ring-blue-400 dark:focus:border-blue-400"
```

**Line 112** (bg-zinc-900):
```
className="w-full rounded-md border border-gray-300 dark:border-zinc-700 bg-white dark:bg-zinc-900 px-3 py-2 outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 dark:focus:ring-blue-400 dark:focus:border-blue-400"
```

**Line 112** (form-control-dark):
```
className="w-full rounded-md border border-gray-300 dark:border-zinc-700 bg-white dark:bg-zinc-900 px-3 py-2 outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 dark:focus:ring-blue-400 dark:focus:border-blue-400"
```

## src/features/client/AddNoteToCleaners.tsx

**Line 106** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 106** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

## src/features/client/ClientDashboard.tsx

**Line 635** (bg-black):
```
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
```

**Line 639** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-4xl w-full p-4"
```

**Line 639** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-4xl w-full p-4"
```

**Line 732** (bg-zinc-900):
```
<div className="mt-2 rounded-md border p-3 text-sm bg-zinc-50 dark:bg-zinc-900">
```

**Line 750** (bg-black):
```
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
```

**Line 754** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-2xl w-full p-4"
```

**Line 754** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-2xl w-full p-4"
```

## src/features/client/ClientJobTimeline.tsx

**Line 358** (bg-black):
```
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
```

**Line 362** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-3xl w-full p-4"
```

**Line 362** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-3xl w-full p-4"
```

## src/features/client/ServicesPage.tsx

**Line 550** (bg-black):
```
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
```

**Line 554** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-md w-full p-4"
```

**Line 554** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-md w-full p-4"
```

**Line 584** (form-control-dark):
```
className="w-full rounded-md border bg-transparent p-2 text-sm"
```

**Line 739** (bg-black):
```
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
```

**Line 743** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-4xl w-full p-4"
```

**Line 743** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-4xl w-full p-4"
```

**Line 846** (bg-zinc-900):
```
<div className="mt-2 rounded-md border p-3 text-sm bg-zinc-50 dark:bg-zinc-900">
```

## src/features/client/SupportPage.tsx

**Line 91** (form-control-dark):
```
className="w-full rounded-md border border-zinc-300 dark:border-zinc-700 bg-transparent px-3 py-2"
```

**Line 108** (form-control-dark):
```
className="w-full rounded-md border border-zinc-300 dark:border-zinc-700 bg-transparent px-3 py-2"
```

## src/features/crm/ClientEditModal.tsx

**Line 60** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 63** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 63** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

## src/features/crm/LeadsPage.tsx

**Line 174** (bg-zinc-900):
```
className="border rounded-md px-2 py-1 bg-white dark:bg-zinc-900 text-xs"
```

**Line 174** (form-control-dark):
```
className="border rounded-md px-2 py-1 bg-white dark:bg-zinc-900 text-xs"
```

**Line 193** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 196** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 196** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 223** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 223** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/crm/LocationEditModal.tsx

**Line 65** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 68** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 68** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

## src/features/crm/NewClientModal.tsx

**Line 86** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 89** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 89** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 128** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 128** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/crm/NewLocationModal.tsx

**Line 158** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 161** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 161** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 235** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 235** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/crm/ServiceAgreementModal.tsx

**Line 135** (bg-black):
```
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50"
```

**Line 139** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-2xl w-full p-4"
```

**Line 139** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-lg shadow-elev-2 max-w-2xl w-full p-4"
```

**Line 282** (form-control-dark):
```
className="w-full rounded-md border bg-transparent p-2 text-sm"
```

**Line 306** (form-control-dark):
```
className="w-full rounded-md border bg-transparent p-2 text-sm"
```

## src/features/employee/JobDetailsModal.tsx

**Line 276** (bg-black):
```
<div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
```

**Line 355** (bg-zinc-900):
```
<div className="text-sm bg-zinc-50 dark:bg-zinc-900 p-3 rounded-md">
```

**Line 382** (bg-zinc-900):
```
<div className="flex items-center justify-between p-4 bg-zinc-50 dark:bg-zinc-900 rounded-lg">
```

**Line 448** (bg-gray-900):
```
<div className="text-xs text-zinc-500 dark:text-zinc-400 bg-gray-50 dark:bg-gray-900/20 border border-gray-200 dark:border-gray-800 rounded p-3">
```

## src/features/employee/JobNotes.tsx

**Line 237** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 237** (form-control-dark):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 259** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 259** (form-control-dark):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 296** (bg-zinc-900):
```
className="rounded-md border border-zinc-200 dark:border-zinc-700 p-3 bg-white dark:bg-zinc-900"
```

**Line 296** (form-control-dark):
```
className="rounded-md border border-zinc-200 dark:border-zinc-700 p-3 bg-white dark:bg-zinc-900"
```

## src/features/employee/LocationSelector.tsx

**Line 91** (bg-zinc-900):
```
className="w-full px-3 py-2 border border-border rounded-md focus:outline-none focus:ring-2 focus:ring-ring bg-white dark:bg-zinc-900"
```

**Line 91** (form-control-dark):
```
className="w-full px-3 py-2 border border-border rounded-md focus:outline-none focus:ring-2 focus:ring-ring bg-white dark:bg-zinc-900"
```

## src/features/employee/PasswordChange.tsx

**Line 98** (bg-zinc-900):
```
className="border rounded-md px-3 py-2 bg-white dark:bg-zinc-900 flex-1"
```

**Line 98** (form-control-dark):
```
className="border rounded-md px-3 py-2 bg-white dark:bg-zinc-900 flex-1"
```

**Line 126** (bg-zinc-900):
```
className="border rounded-md px-3 py-2 bg-white dark:bg-zinc-900 flex-1"
```

**Line 126** (form-control-dark):
```
className="border rounded-md px-3 py-2 bg-white dark:bg-zinc-900 flex-1"
```

**Line 155** (bg-zinc-900):
```
className="border rounded-md px-3 py-2 bg-white dark:bg-zinc-900 flex-1"
```

**Line 155** (form-control-dark):
```
className="border rounded-md px-3 py-2 bg-white dark:bg-zinc-900 flex-1"
```

## src/features/employee/QuickActions.tsx

**Line 39** (bg-zinc-900):
```
className="flex flex-col items-center p-4 rounded-lg border border-border hover:shadow-md transition-all bg-white dark:bg-zinc-900 hover:bg-gray-50 dark:hover:bg-zinc-800"
```

**Line 39** (form-control-dark):
```
className="flex flex-col items-center p-4 rounded-lg border border-border hover:shadow-md transition-all bg-white dark:bg-zinc-900 hover:bg-gray-50 dark:hover:bg-zinc-800"
```

## src/features/employee/TimesheetView.tsx

**Line 578** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 578** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 591** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 591** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 605** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 605** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 619** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 619** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 637** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 637** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/employee/TodaysJobs.tsx

**Line 452** (bg-gray-900):
```
<div className="text-xs text-zinc-500 mt-2 bg-gray-50 dark:bg-gray-900/20 rounded p-2">
```

## src/features/employee/UploadPhotos.tsx

**Line 446** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 446** (form-control-dark):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 505** (bg-zinc-900):
```
<div className="px-3 py-2 rounded-md bg-zinc-50 dark:bg-zinc-900 text-sm">
```

**Line 588** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 588** (form-control-dark):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/finance/AddSupplyModal.tsx

**Line 85** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 88** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 88** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 134** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 134** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/finance/ExpensesPage.tsx

**Line 213** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 216** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 216** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 279** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 279** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/finance/InvoiceEditModal.tsx

**Line 76** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 79** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 79** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 126** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 126** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 134** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 134** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/finance/InvoicesPage.tsx

**Line 258** (bg-zinc-900):
```
className="border rounded-md px-2 py-1 bg-white dark:bg-zinc-900"
```

**Line 258** (form-control-dark):
```
className="border rounded-md px-2 py-1 bg-white dark:bg-zinc-900"
```

**Line 270** (bg-zinc-900):
```
className="border rounded-md px-3 py-1 flex-1 bg-white dark:bg-zinc-900"
```

**Line 270** (form-control-dark):
```
className="border rounded-md px-3 py-1 flex-1 bg-white dark:bg-zinc-900"
```

**Line 276** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900 text-sm"
```

**Line 276** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900 text-sm"
```

**Line 682** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 685** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 685** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 726** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 726** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/finance/InvoicesTab.tsx

**Line 336** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

## src/features/finance/PaymentsPage.tsx

**Line 171** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 174** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 174** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 229** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 229** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/finance/PayrollPrepTab.tsx

**Line 223** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900 border-zinc-300 dark:border-zinc-600"
```

**Line 223** (form-control-dark):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900 border-zinc-300 dark:border-zinc-600"
```

**Line 235** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900 border-zinc-300 dark:border-zinc-600"
```

**Line 235** (form-control-dark):
```
className="w-full px-3 py-2 rounded-md border bg-white dark:bg-zinc-900 border-zinc-300 dark:border-zinc-600"
```

**Line 278** (bg-zinc-900):
```
<div className="grid grid-cols-1 md:grid-cols-4 gap-4 p-4 bg-zinc-50 dark:bg-zinc-900 rounded-lg">
```

## src/features/finance/PayrollRunDetail.tsx

**Line 298** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 298** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 421** (bg-gray-900):
```
: "bg-gray-100 text-gray-800 dark:bg-gray-900 dark:text-gray-200"
```

## src/features/finance/PayrollRunsTab.tsx

**Line 326** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 326** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 335** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 335** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 351** (bg-zinc-900):
```
className={`px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900 ${
```

**Line 431** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 431** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 440** (bg-zinc-900):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 440** (form-control-dark):
```
className="mt-1 w-full px-2 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 485** (bg-zinc-900):
```
className="px-2 py-1 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 485** (form-control-dark):
```
className="px-2 py-1 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 498** (bg-black):
```
<div className="absolute inset-0 bg-black/40" onClick={closeDrawer} />
```

## src/features/hr/EmployeeDetail.tsx

**Line 399** (bg-zinc-900):
```
<div className="rounded-lg p-3 bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-700">
```

**Line 399** (form-control-dark):
```
<div className="rounded-lg p-3 bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-700">
```

**Line 577** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 590** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 590** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 608** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 608** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 621** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 621** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 633** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 633** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 647** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 647** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/hr/EmployeeEditModal.tsx

**Line 64** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 67** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 67** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 114** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 114** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/hr/EmployeeRatesOverview.tsx

**Line 209** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 209** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 218** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 218** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 230** (bg-zinc-900):
```
className="h-10 px-3 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 230** (form-control-dark):
```
className="h-10 px-3 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 302** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 315** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 315** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 336** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 336** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 354** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 354** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 367** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 367** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 379** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 379** (form-control-dark):
```
className="w-full border rounded-md px-3 py-2 bg-white dark:bg-zinc-900"
```

**Line 393** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 393** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/hr/EmployeesList.tsx

**Line 324** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 327** (bg-zinc-900):
```
<div className="absolute top-0 right-0 h-full w-[720px] max-w-[96vw] bg-white dark:bg-zinc-900 shadow-xl translate-x-0 transition-transform">
```

**Line 327** (form-control-dark):
```
<div className="absolute top-0 right-0 h-full w-[720px] max-w-[96vw] bg-white dark:bg-zinc-900 shadow-xl translate-x-0 transition-transform">
```

## src/features/hr/NewEmployeeModal.tsx

**Line 77** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 80** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 80** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 116** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 116** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/inventory/EditItemModal.tsx

**Line 47** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 50** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 50** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 114** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 114** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/inventory/InventoryList.tsx

**Line 94** (bg-zinc-900):
```
className="border rounded-md px-3 py-1 bg-white dark:bg-zinc-900 flex-1"
```

**Line 94** (form-control-dark):
```
className="border rounded-md px-3 py-1 bg-white dark:bg-zinc-900 flex-1"
```

## src/features/inventory/NewItemModal.tsx

**Line 45** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 48** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 48** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 113** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 113** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/inventory/RecordUsageModal.tsx

**Line 47** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 50** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 50** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 102** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 102** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/inventory/RestockModal.tsx

**Line 45** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 48** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 48** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 84** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 84** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/marketing/AudienceBuilder.tsx

**Line 139** (bg-zinc-900):
```
className="w-full border rounded-md px-3 py-2 text-xs font-mono bg-zinc-50 dark:bg-zinc-900"
```

## src/features/marketing/Campaigns.tsx

**Line 232** (bg-zinc-900):
```
className="border rounded-md px-2 py-1 text-sm bg-white dark:bg-zinc-900"
```

**Line 232** (form-control-dark):
```
className="border rounded-md px-2 py-1 text-sm bg-white dark:bg-zinc-900"
```

**Line 290** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 293** (bg-zinc-900):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 293** (form-control-dark):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 382** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 382** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/marketing/Social.tsx

**Line 270** (bg-zinc-900):
```
className="w-full px-3 py-2 rounded-md bg-zinc-100 dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 text-sm"
```

**Line 277** (bg-zinc-900):
```
className="w-full min-h-[100px] px-3 py-2 rounded-md bg-zinc-100 dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 text-sm"
```

**Line 284** (bg-zinc-900):
```
className="px-3 py-2 rounded-md bg-zinc-100 dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 text-sm"
```

## src/features/marketing/Templates.tsx

**Line 251** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 251** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 268** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 271** (bg-zinc-900):
```
<div className="relative w-full max-w-3xl h-[80vh] rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4 overflow-auto">
```

**Line 271** (form-control-dark):
```
<div className="relative w-full max-w-3xl h-[80vh] rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4 overflow-auto">
```

**Line 277** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 277** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/media/AssetDetail.tsx

**Line 349** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 349** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/media/UploadDialog.tsx

**Line 131** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 134** (bg-zinc-900):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 134** (form-control-dark):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 254** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 254** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/notifications/NotificationsCenter.tsx

**Line 72** (bg-zinc-900):
```
className="border rounded-md px-2 py-1 bg-white dark:bg-zinc-900 text-sm"
```

**Line 72** (form-control-dark):
```
className="border rounded-md px-2 py-1 bg-white dark:bg-zinc-900 text-sm"
```

**Line 153** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 156** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 156** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 193** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 193** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/scheduling/MonthView.tsx

**Line 106** (bg-black):
```
className="fixed inset-0 bg-black/40 flex items-end md:items-center md:justify-center"
```

**Line 110** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-t-lg md:rounded-lg p-4 w-full md:w-[480px]"
```

**Line 110** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-t-lg md:rounded-lg p-4 w-full md:w-[480px]"
```

## src/features/scheduling/ScheduleJobModal.tsx

**Line 185** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 188** (bg-zinc-900):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 188** (form-control-dark):
```
<div className="relative w-full max-w-md rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

## src/features/scheduling/WeekView.tsx

**Line 229** (bg-black):
```
className="fixed inset-0 bg-black/40 flex items-end md:items-center md:justify-center"
```

**Line 233** (bg-zinc-900):
```
className="bg-white dark:bg-zinc-900 rounded-t-lg md:rounded-lg p-4 w-full md:w-[480px]"
```

**Line 233** (form-control-dark):
```
className="bg-white dark:bg-zinc-900 rounded-t-lg md:rounded-lg p-4 w-full md:w-[480px]"
```

## src/features/serviceHistory/JobDetail.tsx

**Line 658** (bg-zinc-900):
```
<div className="rounded-md p-3 bg-zinc-50 dark:bg-zinc-900 text-xs text-zinc-600 dark:text-zinc-300">
```

**Line 675** (bg-zinc-900):
```
className="rounded-md p-3 bg-zinc-50 dark:bg-zinc-900"
```

**Line 698** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 698** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 730** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900 text-sm"
```

**Line 730** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900 text-sm"
```

**Line 803** (bg-zinc-900):
```
className="rounded-lg p-3 bg-zinc-50 dark:bg-zinc-900"
```

## src/features/serviceHistory/JobEditForm.tsx

**Line 228** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 228** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 236** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 236** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 266** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 266** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

## src/features/serviceHistory/ServiceHistoryPage.tsx

**Line 50** (form-control-dark):
```
className="rounded-md border border-zinc-300 dark:border-zinc-600 bg-transparent px-2 py-1 text-sm"
```

**Line 59** (form-control-dark):
```
className="rounded-md border border-zinc-300 dark:border-zinc-600 bg-transparent px-2 py-1 text-sm"
```

## src/features/settings/OrgSettings.tsx

**Line 294** (bg-zinc-900):
```
<div className="mt-1 border rounded-md p-3 bg-white dark:bg-zinc-900">
```

**Line 294** (form-control-dark):
```
<div className="mt-1 border rounded-md p-3 bg-white dark:bg-zinc-900">
```

## src/features/support/SupportDetail.tsx

**Line 94** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 94** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 111** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 111** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 141** (bg-zinc-900):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

**Line 141** (form-control-dark):
```
className="w-full border rounded-md p-2 bg-white dark:bg-zinc-900"
```

## src/features/training/AssignmentModal.tsx

**Line 103** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 106** (bg-zinc-900):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 106** (form-control-dark):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 151** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 151** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/features/training/TrainingModuleModal.tsx

**Line 96** (bg-black):
```
className="absolute inset-0 bg-black/40"
```

**Line 99** (bg-zinc-900):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 99** (form-control-dark):
```
<div className="relative w-full max-w-2xl rounded-lg bg-white dark:bg-zinc-900 shadow-elev-3 p-4">
```

**Line 164** (bg-zinc-900):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

**Line 164** (form-control-dark):
```
className="px-3 py-1.5 rounded-md border bg-white dark:bg-zinc-900"
```

## src/styles/tailwind.css

**Line 37** (bg-zinc-900):
```
@apply bg-white text-gray-900 dark:bg-zinc-900 dark:text-zinc-100;
```

